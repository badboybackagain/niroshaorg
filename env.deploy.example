# FTP Deployment Configuration
# Copy this file to .env.deploy and fill in your credentials
# 
# Command: cp env.deploy.example .env.deploy

# Server Details (for deploy-server.sh)
# For SFTP/SSH (recommended)
SERVER_USER=your-username
SERVER_HOST=your-server.com
SERVER_PORT=22
SERVER_PATH=/path/to/your/app

# For FTP (alternative)
FTP_TYPE=ftp
FTP_USER=your-username
FTP_PASS=your-password
FTP_HOST=your-server.com
FTP_PORT=21
FTP_PATH=/public_html

# Deployment Options
INSTALL_DEPS=false  # Set to true to install npm dependencies on server
TEST_HEALTH=false   # Set to true to test health endpoint after deployment
SERVER_URL=https://your-server-url.com  # For health check testing

# Optional: Use SFTP instead of FTP
# FTP_TYPE=sftp

# Note: 
# - FTP_PATH is the path on your server where files should be uploaded
# - Common paths: /public_html, /www, /htdocs, /var/www/html
# - For SFTP, use port 22 and set FTP_TYPE=sftp



