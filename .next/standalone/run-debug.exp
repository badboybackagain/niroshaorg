#!/usr/bin/expect -f

set timeout 120
set host "37.27.54.247"
set user "nirosha_1"
set password "Celorita13jan!!"
set port "22"
set script_path "/tmp/debug-production.sh"

# Copy the script
spawn scp -P $port debug-production.sh $user@$host:$script_path

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    eof
}

# Run the script
spawn ssh -p $port $user@$host "chmod +x $script_path && $script_path"

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    eof
}

expect eof
